package novite.rs.utility.tools;

import java.io.IOException;

import novite.rs.cache.Cache;

public class ItemCheck {

	public static final void main(String[] args) throws IOException {
		Cache.init();
		// int total = 0;
		/*
		 * for (int itemId = 0; itemId < Utils.getItemDefinitionsSize();
		 * itemId++) { if
		 * (ItemDefinitions.getItemDefinitions(itemId).isWearItem() &&
		 * !ItemDefinitions.getItemDefinitions(itemId).isNoted()) { File file =
		 * new File("bonuses/" + itemId + ".txt"); if (!file.exists()) {
		 * System.out.println(file.getName()); total++; } } }
		 * System.out.println("Total " + total);
		 */
	}
}
